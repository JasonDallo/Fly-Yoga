{% extends 'baseadminmail.html.twig' %}
{% block body %}
			<div class="col-sm-9 col-md-10">
				<h4>Mail numéro {{ mail.id }}</h4>
				<h6>De :{{ mail.origin }}</h6>
				<h6>A :{{ mail.usersmails|join(';') }}</h6>
				<h5>{{ mail.title }}</h5>
				<p>{{ mail.content }}</p>

			</div>
{% endblock %}